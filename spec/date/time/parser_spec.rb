require "spec_helper"

describe Date::Time::Parser do
  it "has a version number" do
    expect(Date::Time::Parser::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
